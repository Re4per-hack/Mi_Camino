tr es una herramienta que reemplaza letras, para esto usaremos el siguiente ejemplo:

```
Este texto va a cambiar
```

La forma de reemplazar seria de la siguiente manera:

```shell
cat text.txt | tr 'e' 'a' 
```

El resultado seria:

```
Esta taxto va a cambiar
```

