remmina es una herramienta que nos permite conectarnos a una maquina mediante rdp desde linux, es una herramienta muy grafica, y para usarla es tan simple como usar el comando a secas:

```python
remmina
```