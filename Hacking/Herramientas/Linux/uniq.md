uniq es una herramienta que diferencia entre lineas repetidas y otras que no, ademas de aislarlas para permitir el filtrado de estas, para filtrar por lineas que no se repiten seria:

```shell
uniq -u 
```

