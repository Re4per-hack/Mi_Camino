file es un comando que nos ayuda a ver el tipo de un archivo, por ejemplo, un script en python, si es un archivo humanamente legible, o si es un binario, solo hace falta indicarle el archivo al comando , y este nos va a proporcionar la información:

```ruby
file {ARCHIVO}
```

