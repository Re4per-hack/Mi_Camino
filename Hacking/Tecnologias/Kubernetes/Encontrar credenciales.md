En los pods podemos encotnrar archivos especiales para autenticarse contra la API de kubernetes (con kubectl o cURL), usualmente se pueden encontrar tokens, CA certificates.

- /var/run/secrets/kubernetes.io/serviceacounts

