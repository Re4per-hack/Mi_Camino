Cuando creamos pares de llaves de ssh (privada/publicas), podemos proteger cada llave privada con una contraseña, a esto se le conoce como passphrase:

```ruby
python3 ssh2john.py {llave privada} 
```

