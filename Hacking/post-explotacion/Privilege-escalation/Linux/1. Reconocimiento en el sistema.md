# Buscar en donde se esta sirviendo un servidor

### Apache

```python
ls -la /etc/apache2/sites-enabled
ls -la /etc/apache2/sites-available
```

### Nginx

```python
ls -la /etc/nginx/sites-enabled
ls -la /etc/nginx/sites-available
```


# Identificar entorno docker

- [ ] Existencia de /.dockerenv
- [ ] hostname similar a 821fbd6a43fa
- [ ] maximo 2 procesos (ps aux)

# ==Rutas y archivos interesantes==

- `/var`: Carpeta en donde se suelen guardar backups y logs del sistemas

![[Pasted image 20251016175652.png]]

- `/etc/shadow`: Contraseña de los usuarios 

# ==Analisis de procesos== 

Para esto usualmente se usa la herramienta por defecto del sistema "ps" pero es mucho mejor usar [[pspy]]

# ==Buscar palabras especiales en todo el sistema==

Este comando nos va a ayudar a buscar una palabra especifica por todo el sistema en busca de coincidencias, en este caso "PASSWORD="

```bash
grep --color=auto -Rnw '/' -ie "password" --color=always 2> /dev/null
```


# ==Buscar archivos con nombres especiales==

Con este comando podremos ver todos los archivos que contengan una palabra clave en concreto, esto nos puede ayudar a encontrar contraseñas

```bash
locate password | more
```

#### Busqueda de claves de SSH

Para buscar estas claves podemos usar las palabras clave id_name o authorized_keys

````zsh
find / -name id_name 2> /dev/null
````

[[Ejemplo busqueda SUID's]]


Tambien es importante revisar si tenemos acceso a un determinado usuario para asi apoderarnos de la private key de este, incluyendo root

# ==Ver la version del  sistema==


Para ver la version del sistema podemos usar tanto:


- `uname -a`

como:

- `cat /proc/version`