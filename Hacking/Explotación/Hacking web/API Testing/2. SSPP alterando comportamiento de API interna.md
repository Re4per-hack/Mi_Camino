
En este caso tenemos un servidor con una pagina la cual tiene una función para resetear contraseñas, así se ve una petición de reseteo:

![[Pasted image 20250827232943.png]]


Lo mas probable es que el usuario que se envía esté siendo usando internamente en una API, tal como se ve en el siguiente grafico, y no conocemos que otros parámetros se pasan a la API o si tan siquiera existen mas parámetros para la API interna:


![[Pasted image 20250827233826.png]]

## Averiguando si existe el SSPP

Lo que podemos hacer es revisar si podemos envenenar le parámetro de usuario (parameter pollution), para esto podemos enviar el siguiente payload:

```java
CSRF=....&username=administrator%26test%3dhola
```

Si URL-decodeamos esto nos da como resultado:

```java
CSRF=....&username=administrator&test=hola
```

Pero lo encodeamos para que cuando lo pasemos al backend no lo confunda como un parametro mas, por ende, cuando el backend lo decodé, y lo envié a la API sea como un parametro mas, de tal forma que quedará así

```java
username=administrator&test=hola // ?? no sabemos que hay despues ?? 
```

Al enviar esto veo que el servidor me devuelve la respuesta de la API:

![[Pasted image 20250828000659.png]]


## Averiguar los campos necesarios para la API 

Lo cual nos da a entender que el parámetro se está inyectando correctamente, ya con esto en cuenta empecemos a descubrir cual o cuales son los parámetros extras que no conocemos, y para saber esto podemos usar el signo '#' que comenta todo lo que esté por delante, entonces ahora el payload equivaldría a esto:

```java
CSRF=....&username=administrator%23
```

El servidor me responde de la siguiente forma: 

![[Pasted image 20250828002300.png]]

Parece que la API necesita el parametro "field", por lo que vamos a ponerlo y darle un valor aleatorio:


![[Pasted image 20250828105838.png]]

Y obtenemos una respuesta diciendonos que el valor que le dimos a "field" no es valido, por lo que haremos fuerza bruta para descubrir que le puede gustar:



![[Pasted image 20250828105849.png]]


Ahora podemos fuzzear usando ffuf de la siguiente forma

```shell
ffuf -w wordlists/subdirectorios/big_wordlist.txt:FUZZ -u 'https://0aae00be03c6936681c70c460048005f.web-security-academy.net/forgot-password' -b 'session=qKsoZRbCMK8JE09U70bvA51sqjbjlUNO' -d 'csrf=VJjyjzRTamkk
erzMkvddpCTqK759qzam&username=administrator%26field%3dFUZZ'
```

![[Pasted image 20250828112441.png]]

Perfecto, ya sabemos que tanto "email" como "username" son valores validos para el parametro "field", ahora vemos como son las respuestas:

En caso de usar "email"

![[Pasted image 20250828113738.png]]

En caso de usar "username"

![[Pasted image 20250828114134.png]]


Esto nos puede hacer intuir que al modificar el parametro  "field" nos devuelve un campo especifico de este usuario, intenté devolver la contraseña pero fue inutil, así que busqué por otro campo que tambien podia ser interesante, y es que lo mas probable es que está API no guarde ninguna contraseña y solo se encargue de manejar los reset token, así qeu busqué a ver si en el frontend habia alguna pista para encotnrar este campo, y efectivamente  en este script que se encarga de corregir una falla ortografica lo encuentro: 


![[Pasted image 20250828114434.png]]

Vemos que se llama "reset_token", por lo que solo tenemos que obtenerlo y luego usarlo, este "reset token" es el que llega al correo electronico y normalmente se usa para cambiar de forma segura la contraseña pero en este caso lo pudimos exfiltrar y lo pondremos manualmente en la URL:


![[Pasted image 20250828115302.png]]

