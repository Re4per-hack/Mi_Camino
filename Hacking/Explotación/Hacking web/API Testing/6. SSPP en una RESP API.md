
Antes de seguir, revisar el siguiente documento:

[[0. Que es una REST API]]

y 

[[2. SSPP alterando comportamiento de API interna]]


## Detección 

Este es un caso un poco diferente a cuando tenemos un SSPP (Server-Side Parameter Pollution)  ya que en este caso nuestro input no está directamente en un parametro, sino en una parte de una ruta, mientras que antes nuestro envenamiento cuando lleva a la API alteraba esta parte:

![[Pasted image 20250901221901.png]]

Y la explotacion consistia en URL-encodear los caracteres "&", "#", "="  para agregar y modificar los parametros.

Despues de este pequeño recorderis, podemos continuar viendo esta variacion de REST API, en la cual lo que modificamos no es lo anterior visto, sino aquí:

![[Pasted image 20250901222616.png]]

Los signos de interrogacion están ahí ya que no conocemos mas partes de la url aparte de el input que estamos poniendo

## Deteccion y diferenciación

Tenemos que no solo detectar esta vulnerabilidad, sino que tambien debemos diferenciarla de el primer caso de SSPP (Server-Side Parameter Pollution)

Primero deberemos comentar todo lo que esté delante para saber si hay contenido por delante, lo cual es muy probable, de tal forma que el payload quede algo así:

![[Pasted image 20250901225330.png]]

Y veremos una repsuesta como esta:

![[Pasted image 20250901225424.png]]

Esto nos hace pensar que delante de administrador se están agregando otras rutas para el procesamiento de la API, un ejemplo es el siguiente (lo ***==azul==*** es lo ingresado por el atacante, lo ==***verde***== es lo que está por parte del servidor y lo ***==blanco==*** no se interpretará):

![[Pasted image 20250901231206.png]]

Ahora deberemos empezar a descubrir como se compone esta URL para ello tenemos que usar signos como "../" para irnos devolviendo hasta encontrar una respuesta interesante, de la siguiente manera:

![[Pasted image 20250901232528.png]]

En este caso llegué hasta la raiz de la pagina y me respondio con lo siguiente:

![[Pasted image 20250901232608.png]]

Como podemos ver no me dio mucha información por lo que podemos intentar hacer fuerza bruta con el siguiente wordlist en busca de documentación (mi wordlist):




Usando este archivo pude encontrar algo diferente: 

![[Pasted image 20250902001449.png]]

Vemos que es un documento de informacion en formato openapi/swagger y nos dice como usar esta API, ahora intentemos ir a esta ruta:



Vemos que el servidor nos responde con el campo elegido, cambiemoslo por algo mas comprometedor, esto es similar al caso de [[2. SSPP alterando comportamiento de API interna]] como estamos en un campo de olvidar la contraseña busqué por posibles nombres y encontré esto en el código:



Y efectivamente funcionó:





















